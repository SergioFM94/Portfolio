const LineaColor = ({ color }) => (
    <hr
        style={{
            color: color,
            backgroundColor: color,
            height: '1px',
            width: '90%'
        }}
    />
);

export default LineaColor